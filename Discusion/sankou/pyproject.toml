[project]
name = "cc-orchestration"
version = "0.1.0"
description = "Claude Agent SDK minimal examples"
requires-python = ">=3.10"
dependencies = [
    "claude-agent-sdk",
    "anyio",
]

[dependency-groups]
dev = []

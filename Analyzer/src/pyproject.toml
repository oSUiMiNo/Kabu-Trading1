[project]
name = "kabu-trading-src"
version = "0.1.0"
description = "株銘柄評価ツール"
requires-python = ">=3.10"
dependencies = [
    "claude-agent-sdk>=0.1.40",
    "anyio",
    "pyyaml",
    "postgrest",
    "openai>=2.20",
    "openai-codex-sdk>=0.1",
]

[dependency-groups]
dev = []

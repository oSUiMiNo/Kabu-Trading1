[project]
name = "src"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.14"
dependencies = [
    "claude-agent-sdk>=0.1.30",
    "anyio",
    "pyyaml",
    "python-dotenv",
    "postgrest",
]
